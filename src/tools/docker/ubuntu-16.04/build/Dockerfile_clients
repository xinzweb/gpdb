FROM ubuntu_ent_build:16.04_copylibs

COPY . /root/gpdb/

WORKDIR /root/gpdb/gpAux

ENV CLIENTINSTLOC=${INSTLOC}/greenplum-clients

RUN BLD_TARGETS="clients" make clients INSTLOC=${INSTLOC} CLIENTINSTLOC=${CLIENTINSTLOC}
